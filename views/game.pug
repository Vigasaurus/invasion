extends layout-no-menu

block content
	if username
		section#game-container(class=`username-${username}`)
		script.
			var settings = {
				enabledTimestamp: #{enabledTimestamp}
			}
	else
		section#game-container